#include <stdio.h>
#include <stdlib.h>

int main(){
    printf("char = %ld bytes \n", sizeof(char));
    printf("int = %ld bytes \n", sizeof(int));
    printf("float = %ld bytes \n", sizeof(float));
    printf("double = %ld bytes \n", sizeof(double));
    return 0;
}